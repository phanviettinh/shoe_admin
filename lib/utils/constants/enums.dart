enum ProductType { single, variable }

enum TextSizes { small, medium, large }

enum OrderStatus { pending, processing, shipped, cancelled , received }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCar,
  payStack,
  razorPay,
  paytm
}
